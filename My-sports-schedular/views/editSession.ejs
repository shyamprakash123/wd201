<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="csrf-token" content="<%= csrfToken %>">
    <script src="https://cdn.tailwindcss.com"></script>
    <title><%= sportName %></title>
</head>
<body>
    <div class="grid grid-cols-6">
        <div class="col-start-3 col-span-2">
            <a href="/sports" class="underline hover:underline-offset-4 underline-offset-2 text-lg font-semibold text-teal-600 mt-2">Home</a>
            <h6 class="py-4 py-4 text-3xl font-semibold text-slate-600 mt-3">Edit <%= sportName %> Session</h6>
            <form action="/sports/editsession/<%= sportName %>/<%= session[0].id %>" method="post">
                <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                <div class="py-3">
                    <label for="dateTime">When will this sport session be held?</label>
                <input type="datetime-local" min="<%= date %>" value="<%= session[0].dateTime.toISOString().slice(0, 16) %>" name="dateTime" class="border border-gray-300 text-gray-900 rounded p-2 w-full">
                </div>
                <div class="py-3">
                    <label for="place">Where will this session be played?</label>
                <input type="text" name="place" value="<%= session[0].place %>" class="border border-gray-300 text-gray-900 rounded p-2 w-full">
                </div>
                <div class="py-3">
                    <label for="members">Who all will be joining you in this session?</label>
                <input type="text" name="members" value="<%= session[0].members %>" class="border border-gray-300 text-gray-900 rounded p-2 w-full">
                </div>
                <div class="py-3">
                    <label for="players">How many more players do you need?</label>
                <input type="number" name="players" value="<%= session[0].players %>" class="border border-gray-300 text-gray-900 rounded p-2 w-full">
                </div>
                <div class="py-3">
                <button type="submit" class="text-white bg-green-600 hover:bg-green-800 font-medium rounded px-5 py-2">Update</button>
                </div>
            </form>
            <div style="margin-bottom: 20px">
        <% if(messages && messages.error && messages.error.length > 0)  { %>
        <% for(var i=0; i<messages.error.length; i++) { %>
        <li style="color: red;"> <%= messages.error[i] %></li>
        <% } %>
        <% } %>
        </div>
        </div>
    </div>
</body>
</html>